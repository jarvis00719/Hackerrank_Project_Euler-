##Q- Starting in the top left corner of a "2x2" grid, and only being able to move to the right and down, there are exactly '6' routes to the bottom right corner.
# How many such routes are there through a NxM grid? 
# As number of ways can be very large, print it modulo {10^9 + 7}.

import numpy as np


#!! Input Statements
test_cases = int(input().strip())
multiple_inputs = (input().split())

for _ in range(test_cases):
    n = multiple_inputs[0]
    m = multiple_inputs[1]






print(multiple_inputs[0], multiple_inputs[1])
  
